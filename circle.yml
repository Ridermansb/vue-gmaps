machine:
  node:
    version: 4.2.2

deployment:
  production:
    branch: master
    commands:
      - npm install --dev
      - npm i -g semantic-release-cli
      - npm run semantic-release || true