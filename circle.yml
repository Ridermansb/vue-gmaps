machine:
  node:
    version: 0.10.22

deployment:
  production:
    branch: master
    commands:
      - npm install --dev
      - npm i -g semantic-release-cli
      - npm run semantic-release || true